-- Get information about conected device --
adb shell dumpsys bluetooth_manager |  grep -A1 Bonded


-- Get infromation about bluetooth --
adb shell dumpsys bluetooth_manager


-- Bluetooth to disable - need permition --
adb shell service call bluetooth_manager 8


-- Bluetooth to disable - need enable --
adb shell service call bluetooth_manager 8
